global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aurum-backend
    metrics_path: /metrics
    # Development compose uses an open internal endpoint. In staging/production
    # add an authorization.credentials_file that contains METRICS_TOKEN.
    static_configs:
      - targets: ["backend:8000"]

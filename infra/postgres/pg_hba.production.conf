# Local administration is reachable only inside the PostgreSQL container.
local   all             all                                     trust
host    replication     aurum_pitr      0.0.0.0/0               scram-sha-256
host    replication     aurum_pitr      ::/0                    scram-sha-256
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::/0                    scram-sha-256
